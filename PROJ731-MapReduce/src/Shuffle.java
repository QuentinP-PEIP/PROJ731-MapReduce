import java.util.HashMap;

public class Shuffle {
	
	public HashMap<String, Integer>[] tab_hash;
	
	public Shuffle(HashMap<String, Integer>[] tab_hash) {
		this.tab_hash = tab_hash;
	}
	
	//on parcourt le tableau de hash et on en crée deux (bn de reduce) en faisant en associant les clés : clé ; 2,3

}
